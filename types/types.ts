export type LinkValues = {
  name: string;
  url: string;
  id: string;
  categoryId: string;
  userId: string;
  createdAt: Date;
  linkCategory: any;
};

export type CategoryValues = {
  id: string;
  name: string;
  slug: string;
};
